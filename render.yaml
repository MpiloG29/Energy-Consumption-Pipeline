services:
  - type: web
    name: energy-consumption-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run energy_pipeline_project/dashboards/streamlit_app.py --server.port=$PORT --server.address=0.0.0.0
    autoDeploy: true